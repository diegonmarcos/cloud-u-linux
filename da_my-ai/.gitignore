target/
*.deb
*.AppImage
result
.direnv/
Cargo.lock
src-tauri/gen/

# Upstream block/goose reference checkout (see vendor/README.md).
# Read-only provenance for the vendored SDK zod schema — never committed:
# the full upstream clone is ~642MB, ~95% of it marketing videos and their history.
vendor/goose-upstream/
