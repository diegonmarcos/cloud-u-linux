{
  "_comment": "SINGLE SOURCE OF TRUTH for this plugin's hook rules. Consumed by hook-engine.sh (enforce), gen-hooks-doc.sh (docs), test-hooks.sh (fixtures). NEVER hand-edit deployed copy — edit here + build.sh switch. Owns steady-state context compression: per-turn/per-bash one-liners instead of re-injecting full principles every turn.",
  "meta": {
    "version": 1
  },
  "rules": [
    {
      "id": "inject-principles-oneline",
      "level": "inject",
      "fragment": "hooks-fragments/principles-oneline.md",
      "tiers": [
        "PreToolUse:Bash"
      ],
      "reason": "Per-turn/per-bash one-line pointer; full principles load once at SessionStart (hooks-stack-framework-principles-cloud-ai-plugin) to keep steady-state context small."
    }
  ],
  "_reserved": {
    "core-principles-compact.md": "Compact SessionStart alternative to the full core-principles.md fragment — not wired to a rule yet. Reserved for a future low-token SessionStart mode."
  }
}
