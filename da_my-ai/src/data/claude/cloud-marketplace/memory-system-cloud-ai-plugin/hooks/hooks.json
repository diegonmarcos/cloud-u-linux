{
  "description": "Data-driven hook engine — SessionStart-only inject of the memory structure rule. See scripts/hook-engine.sh.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/scripts/hook-engine.sh inject SessionStart",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
