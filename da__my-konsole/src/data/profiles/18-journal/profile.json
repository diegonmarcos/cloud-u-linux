{
  "order": 12,
  "name": "journal",
  "display_name": "Journal",
  "menu": "Unix-Dev",
  "theme": { "accent": "#fdcb6e" },
  "sections": [
    {
      "title": "System",
      "items": [
        { "label": "Boot log", "cmd": "journalctl -b --no-pager | tail -200" },
        { "label": "Errors this boot", "cmd": "journalctl -b -p err --no-pager" },
        { "label": "Follow", "cmd": "journalctl -f" },
        { "label": "Disk usage", "cmd": "journalctl --disk-usage" }
      ]
    },
    {
      "title": "User units",
      "items": [
        { "label": "Failed units", "cmd": "systemctl --user --failed" },
        { "label": "User journal", "cmd": "journalctl --user -b --no-pager | tail -200" },
        { "label": "Unit log", "cmd": "journalctl --user -u " }
      ]
    }
  ],
  "group": "g3"
}
